//@ 로그인 페이지에서 작동하는 JS
//@ Authentication strategies (인증전략)
//@ Application middleware (미들웨어)
'use strict';

const passport = require('passport');

router.post('/login', );

passport.authenticate('')

